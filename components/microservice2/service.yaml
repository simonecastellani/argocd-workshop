apiVersion: v1
kind: Service
metadata:
  name: microservice2-service
  namespace: microservices
spec:
  selector:
    app: microservice2
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP